spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/authentication-service-db
    username: postgres
    password: password

jwt:
  permit:
    all:
      - /swagger-ui/**
      - /swagger-resources/*
      - /v3/api-docs/**
      - /v1/user/registration
      - v1/auth/sign-in